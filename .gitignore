# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — never commit. .env.test.local holds the live-test GitHub PAT + Claude key.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code — local session data, worktrees, brainstorm files
.claude/worktrees/
.claude/scheduled_tasks.json
.superpowers/

# Claude Code settings are committed, but not local session state
# .claude/settings.json IS committed (project permissions)
